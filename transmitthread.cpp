#include "transmitthread.h"
#include <cstdlib>
#include <ctime>
#include <windows.h>

void fnTransmitIdle(st_transmit* lpTransmit)
{
    using namespace std;
    srand(time(NULL));

    if(transmit.reset == true)
    {
        //Sleep(rand())

    }
}